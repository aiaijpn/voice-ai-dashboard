栄一ツール開発でよく使う流れ
普段はこの4つです。
git status               状態確認（最も使う）
git add .                全部追加
git commit -m "message"   commit（履歴保存）
git push                  GitHubへ送る

git pull  GitHubから取得

git log    commit履歴を見る

git remote -v  remote確認
GitHub接続先確認。

